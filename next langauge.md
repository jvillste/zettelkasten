* main argument always as the last argument
  * currying works
  * we only need one threading macro
* core.match -matching
* default currying
  * in a dynamic language you would accidentially call wrong arity
